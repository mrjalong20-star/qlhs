import { Exam } from "../types";

// Giáo viên tạo đề thi từ giao diện quản trị.
// Không có đề thi mẫu nào được preload.
export const DEFAULT_EXAMS: Exam[] = [];
export const mockExams = DEFAULT_EXAMS;
